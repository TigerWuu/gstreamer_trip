# f"{src} ! videoconvert ! videoscale ! video/x-raw,width={w},height={h} ! x264enc tune=zerolatency byte-stream=true bitrate=3000 threads=2 ! h264parse config-interval=1 ! rtph264pay ! udpsink host={IP} port={port}

appsrc ! videoconvert ! videoscale ! video/x-raw,width=640,height=480 ! x264enc tune=zerolatency byte-stream=true bitrate=3000 threads=2 ! h264parse config-interval=1 ! rtph264pay ! tcpserversink host=127.0.0.1 port=8000